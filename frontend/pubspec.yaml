name: haazir
description: AI-Powered Home Services Platform for Pakistan.
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.1.0
  google_fonts: ^6.1.0
  provider: ^6.1.1
  intl: ^0.18.1
  lottie: ^2.7.0
  flutter_spinkit: ^5.2.0
  flutter_svg: ^2.0.9
  flutter_map: ^6.1.0
  latlong2: ^0.9.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/haazir_logo.svg
